		// variável númerica que converte em letra
		// char cabe em um inteiro, inteiro não cabe em char
		// em contas com double.. sempre usar as casas decimais para o valor resultante seja correto
				//System.out.print imprime na mesma linha / com ln pula linha
// escopo de uma variavel existe entre suas chaves
		// Testando inteiro com flutuante
		//utilizando casting "confirmando a variavel"
		// int(32bits) cabe desde 2bi positivos ou negativos, se necessário guardar um numero maior q isso usar  long(64bits), para 16bits usar short, e 8 bit usar o byte
		
// || significa "ou" , && significa "e"
// boolean só pode ser = a "true" ou "false" ou uma variavel que sua resultante de uma resposta true ou false por exempo "quantidadedePessoas >=2;""


// (< [menor], > [maior], <= [menor ou igual], >= [maior ou igual], == [igual a] e != [diferente de]) e qualquer operador lógico (&& [and], || [ou]).

//Símbolo *= é utilizado para atribuir a uma variável o valor desta variável multiplicado com o valor de um operando

// métodos são comportamentos
// quanto há if tem que ter um return true e um return false